# Style — manifest & doc conventions

See [AGENTS.md](../../AGENTS.md) for the canonical rules. Highlights:

- Edit `.claude-plugin/marketplace.json` (the Claude manifest) only; the Codex
  and Cursor manifests are generated by `make generate`.
- Plugin entries use `"source": { "source": "github", "repo": "...", "ref": "main" }`.
  Never change the source type.
- Keep plugin descriptions to one line. Since Claude Code 2.1.265, the `/plugin`
  Discover/Browse view prefers a marketplace entry's own `description` over the
  plugin's own `plugin.json` description — so the `description` field in
  `.claude-plugin/marketplace.json` is what a browsing user actually sees for a
  plugin from this catalog, not merely informational metadata. Keep it accurate
  and current.
- Every new plugin needs: a manifest entry, a README table row, and a
  `CHANGELOG.md` entry under `[Unreleased]`.
